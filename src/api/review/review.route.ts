
  import "./review.openapi";

  import express, { type Router } from "express";

export const review: Router = express.Router();

// TODO: Add your routes here
// Example:
// review.get("/", handler);
// review.post("/", validateBody(schema), handler);
