import "./delivery.openapi";

import express, { type Router } from "express";

export const delivery: Router = express.Router();

// TODO: Add your routes here
// Example:
// delivery.get("/", handler);
// delivery.post("/", validateBody(schema), handler);
