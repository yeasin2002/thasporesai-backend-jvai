
  import "./common.openapi";

  import express, { Router } from "express";

export const common: Router = express.Router();

// TODO: Add your routes here
// Example:
// common.get("/", handler);
// common.post("/", validateBody(schema), handler);
