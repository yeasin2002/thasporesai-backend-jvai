
  import "./experience.openapi";

  import express, { type Router } from "express";

export const experience: Router = express.Router();

// TODO: Add your routes here
// Example:
// experience.get("/", handler);
// experience.post("/", validateBody(schema), handler);
