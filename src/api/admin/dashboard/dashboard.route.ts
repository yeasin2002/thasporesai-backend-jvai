
  import "./dashboard.openapi";

  import express, { Router } from "express";

export const dashboard: Router = express.Router();

// TODO: Add your routes here
// Example:
// dashboard.get("/", handler);
// dashboard.post("/", validateBody(schema), handler);
