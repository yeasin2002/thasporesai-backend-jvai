
  import "./settings.openapi";

  import express, { Router } from "express";

export const settings: Router = express.Router();

// TODO: Add your routes here
// Example:
// settings.get("/", handler);
// settings.post("/", validateBody(schema), handler);
