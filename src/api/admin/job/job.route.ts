
  import "./job.openapi";

  import express, { Router } from "express";

export const adminJob: Router = express.Router();

// TODO: Add your routes here
// Example:
// job.get("/", handler);
// job.post("/", validateBody(schema), handler);
