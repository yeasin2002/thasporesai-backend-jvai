
  import "./payments.openapi";

  import express, { Router } from "express";

export const payments: Router = express.Router();

// TODO: Add your routes here
// Example:
// payments.get("/", handler);
// payments.post("/", validateBody(schema), handler);
