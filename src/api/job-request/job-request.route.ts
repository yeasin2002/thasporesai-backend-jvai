
  import "./job-request.openapi";

  import express, { Router } from "express";

export const jobRequest: Router = express.Router();

// TODO: Add your routes here
// Example:
// jobRequest.get("/", handler);
// jobRequest.post("/", validateBody(schema), handler);
