
  import "./offer.openapi";

  import express, { type Router } from "express";

export const offer: Router = express.Router();

// TODO: Add your routes here
// Example:
// offer.get("/", handler);
// offer.post("/", validateBody(schema), handler);
