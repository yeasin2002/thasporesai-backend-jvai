import "./bidding.openapi";

import express, { type Router } from "express";

export const bidding: Router = express.Router();

// TODO: Add your routes here
// Example:
// bidding.get("/", handler);
// bidding.post("/", validateBody(schema), handler);
