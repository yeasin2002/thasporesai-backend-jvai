# Database Configuration
DATABASE_URL=mongodb://localhost:27017/providus_org

# Server Configuration
PORT=4000
API_BASE_URL=http://localhost:4000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# JWT Configuration
ACCESS_SECRET=your-access-secret-key-change-in-production
REFRESH_SECRET=your-refresh-secret-key-change-in-production

# Email Configuration (Gmail SMTP)
# Note: Gmail has sending limits (about 500 emails/day)
# For Gmail: Enable 2FA and create an App Password
# Guide: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Firebase Configuration (Push Notifications)
# Place your firebase-service-account.json file in the project root
# Download from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key

ENABLE_MONITORING=true
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
# The service account JSON file contains all necessary credentials
# No additional environment variables needed for Firebase
